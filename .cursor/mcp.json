{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": [
        "-y",
        "firebase-tools@latest",
        "mcp",
        "--dir",
        "F:/GitHub/DocuMate/documate",
        "--only",
        "auth,firestore,storage"
      ]
    }
  }
}
